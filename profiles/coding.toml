name = "coding"
description = "Software development assistant (default). Project-scoped memory, full built-in tools, coding persona."
extends = "base"

[persona]
adapter = "coding"

[memory]
adapter = "project-scoped"

[session]
adapter = "workspace"

[tools]
adapter = "composite"

[overrides]
default_plan_mode = false
